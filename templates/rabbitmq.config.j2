[
  {rabbit, [
    {% if rabbitmq_group in groups -%}
    {cluster_nodes, {[
      {%- for host in groups[rabbitmq_group] -%}
      'rabbit@{{ hostvars[host].ansible_hostname }}'{% if not loop.last %}, {% endif %}
      {%- endfor -%}
    ], disc}},
    {%- endif %}

    {loopback_users, []}
  ]}
].
